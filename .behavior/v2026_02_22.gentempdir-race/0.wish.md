wish =

there's a race condition today on genTempDir across parallel workers. if we spot that error, we should idempotently succeed


    EEXIST: file already exists, symlink '/tmp/test-fns/khlone/.temp' -> '/home/runner/work/khlone/khlone/.temp/genTempDir.symlink'

       6 |
       7 | const SLUG_HAIKU = 'claude@anthropic/claude/haiku';
    >  8 | const CWD = genTempDir({ slug: 'braincli-interact' });
         |                       ^
       9 | const CONTEXT = {
      10 |   cwd: CWD,
      11 |   ...genContextBrainAuthAnthropic({

