wish =

--progress $pathToDir (e.g., .log/test/progress/integration)

should invoke our custom reporter

which logs 

1. `$pathToDir/progress.overview.jsonl`, which tracks { passed: number, failed: number, remains: number, outof: number, duration: IsoTimeStamp }
progress jsonls, to track an overview of progress, once per second maybe? 

and then a second file

2. `$pathToDir/progress.failures.jsonl`, which tracks the { path, suite, stdout, stderr }

of every failure

---

that way, folks can run tests with their --progress .log/test/progress/integration/

and then check on the progress and failures programatically

e.g., to make visable via sh skills that will format it into a nice stdout or do something on fialures detected
